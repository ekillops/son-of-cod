@{
    Layout = "~/Views/Shared/_Layout.cshtml";
    ViewBag.Title = "Account";
}

@using System.Security.Claims

<h3>Authentication with Identity</h3>

@if (User.Identity.IsAuthenticated)
{
    <p>Hello @User.Identity.Name</p>
}
else
{
    <p>No user logged in</p>
}